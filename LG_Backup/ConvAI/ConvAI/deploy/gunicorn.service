[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User={user name}
Group={group name}
WorkingDirectory={project directory}
ExecStart={project directory}/env/bin/gunicorn \
            --bind unix:{project directory}/gunicorn.sock \
            interface.wsgi:application

[Install]
WantedBy=multi-user.target
